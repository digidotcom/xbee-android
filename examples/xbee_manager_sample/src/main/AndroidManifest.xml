<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digi.xbee.sample.android.xbeemanager"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="com.digi.android.permission.SERIAL" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/digi_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" android:name=".XBeeManagerApplication">
        <uses-library
            android:name="com.digi.android"
            android:required="false" />

        <activity
            android:name=".XBeeConnectActivity"
            android:screenOrientation="landscape" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".XBeeTabsActivity"
            android:screenOrientation="landscape" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>
